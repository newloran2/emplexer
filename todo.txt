listagem:
    Adicionar filtros acumulados como na versão da web

templates:

suporte para json encadeado. OK
modo de adicionar um elemento condicionalmente: ex colocar um icone especifico caso o tipo do item seja movie.
o templateManager precisa estar desatrelado dos campos do plex, pois outras telas precisarão utilizar-lo. OK


subtitleManager:
    Criar classe abstrata para download de legendas.
    Essa classe deve prover uma lista de metodos abstratos que seriam a parte de comunicação com o servidor.
    Essa classe deve prover uma lista de metodos que seriam usadas por todas (ou maioria) das classes que a implementarem (ex: criação do hash do arquivo.)


PlayBackManager:
    Criar classe que centralize o parse e qualquer modificação de url para playback.

FileSystemManager:
    Criar estruturas de classes para gerenciar montagem e navegação no dune por file system tanto remoto quanto local

Trailers:
	Adicionar uma função para ver o trailer de um filme
	Uma possibilidade é adicionar uma interface para obtenção de trailers de diversas fontes como youtube, vimeo e apple trailers.

httpfs:
    user o seguinte comando para montar  o device fuse mknod /dev/fuse c 10 229

ImageTranscodeManager:
    Usar libgd juntamente com o lem para poder processar imagens dentro do dune (OK)
    Fazer um módulo lua que use o libgd e leia um template json para gerar uma imagem(50%)

TrascodeSessionManager:
    Criar classe que possa ser configurada atravéz de algum arquivo de configuração para gerenciar sessões transcodificadas do plex.
    Esse arquivo de config tem que indicar condicionais com base no xml de detalhe do video para decidir se irá fazer uma sessão transcodificada ou não.
    Coisas como (se o audio codec for aac transcodificar deve ser possivel)
    Mais exemplos:
        if (vcodec == hi10p) transcode
        if (container == newmkvcontainernotsupportedbydune) transcode
        if (vcodec == hi10p || sub == ass) transcode
        
